# Production Runtime Dependencies
openpyxl>=3.1.2  # Extracts data from Excel purchase orders natively
keyring>=24.0.0  # Native host OS credential manager access
pyinstaller>=6.0 # Produces self-contained executables for distribution
